@model List<Proiect_DAW.Models.ProductOrder>

<style>
    /* General Container Styling */
    .container {
        background-color: #f9f1fc; /* Light purple background */
        border-radius: 10px;
        padding: 30px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    /* Title Styling */
    h2 {
        color: #8e44ad; /* Darker purple for the title */
        font-family: 'Arial', sans-serif;
        text-align: center;
        margin-bottom: 20px;
    }

    /* Table Styling */
    table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
    }

    table th, table td {
        padding: 12px;
        text-align: center;
    }

    /* Table Header Styling */
    thead {
        background-color: #9b59b6; /* Lighter purple for the header */
        color: white;
        font-weight: bold;
    }

    /* Table Row Styling */
    tbody tr:nth-child(even) {
        background-color: #f1e6f4; /* Light lavender for even rows */
    }

    tbody tr:nth-child(odd) {
        background-color: #ffffff; /* White for odd rows */
    }

    /* Button Styling */
    .btn {
        padding: 10px 20px;
        border-radius: 5px;
        border: none;
        cursor: pointer;
        font-size: 14px;
        transition: background-color 0.3s ease;
    }

    .btn-danger {
        background-color: #e74c3c; /* Red for the delete button */
        color: white;
    }

    .btn-danger:hover {
        background-color: #c0392b; /* Darker red on hover */
    }

    .btn-success {
        background-color: #8e44ad; /* Dark purple for the order button */
        color: white;
    }

    .btn-success:hover {
        background-color: #7d3c8c; /* Darker purple on hover */
    }

    /* Empty Cart Message */
    p {
        font-size: 18px;
        text-align: center;
        color: #8e44ad;
        font-weight: bold;
    }
</style>

<div class="container my-5">
    <h2>Coșul meu</h2>

    @if (Model.Count > 0)
    {
        <table class="table">
            <thead>
                <tr>
                    <th>Produs</th>
                    <th>Cantitate</th>
                    <th>Preț</th>
                    <th>Acțiuni</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Product.Title</td>
                        <td>@item.Quantity</td>
                        <td>@(item.Product.Price * item.Quantity) RON</td>
                        <td>
                            <form method="post" asp-action="RemoveFromCart">
                                <!-- Hidden input for cartItemId and orderId -->
                                <input type="hidden" name="cartItemId" value="@item.Id" />
                                <input type="hidden" name="orderId" value="@item.OrderId" />
                                <button class="btn btn-danger" type="submit">Șterge</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <form method="post" asp-action="PlaceOrder">
            <button class="btn btn-success" type="submit">Plasează Comanda</button>
        </form>
    }
    else
    {
        <p>Coșul tău este gol.</p>
    }
</div>
